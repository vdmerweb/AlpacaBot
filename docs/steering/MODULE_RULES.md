# Module Rules

## Purpose

Define the expectations for module design, documentation, and boundaries.

## Rules

- Each module has a clear purpose.
- Each module exposes a public interface.
- Each module lists dependencies and responsibilities.
- Each module has a design note or ADR for non-trivial decisions.
- Each module should be testable in isolation.
